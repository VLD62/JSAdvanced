function stringLength(first, second, third){
    console.log(Math.round(first.length + second.length + third.length))
    console.log(Math.round((first.length + second.length + third.length)/3))
}

stringLength('chocolate', 'ice cream', 'cake')